type Customer @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  email: String!
  username: String
}

type VinylRecord @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  stock: Int!
  year: Int!
  artist: String!
  price: Float!
  pictureS3URL: AWSURL
}
 